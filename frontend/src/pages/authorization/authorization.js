import styled from "styled-components";

const AuthorizationContainer = ({ className }) => {
  return <div className={className}>Authorization Page</div>;
};

export const Authorization = styled(AuthorizationContainer)``;
