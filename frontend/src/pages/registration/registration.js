import styled from "styled-components";

const RegistrationContainer = ({ className }) => {
  return <div className={className}>Registration Page</div>;
};

export const Registration = styled(RegistrationContainer)``;
